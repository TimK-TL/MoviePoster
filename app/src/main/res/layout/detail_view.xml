<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:layout_marginEnd="@dimen/margin_text_standard"
        android:layout_marginRight="@dimen/margin_text_standard"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/no_internet" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:foregroundGravity="top"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/imageView"
        app:layout_constraintVertical_bias="0.0">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:gravity="top"
            android:orientation="vertical"
            android:textAlignment="center">

            <include layout="@layout/details_tv_bold"
                android:id="@+id/tv_title"
                app:layout_constraintTop_toBottomOf="@+id/imageView"
                />

            <include layout="@layout/details_tv_italic"
                android:id="@+id/tv_release_date"
                app:layout_constraintTop_toBottomOf="@+id/textView4"
                />

            <include layout="@layout/details_tv_italic"
                android:id="@+id/tv_vote_average"
                app:layout_constraintTop_toBottomOf="@+id/tv_plot_synopsis"
                />

            <include layout="@layout/details_tv_normal"
                android:id="@+id/tv_plot_synopsis"
                app:layout_constraintTop_toBottomOf="@+id/tv_title"
                />


        </LinearLayout>
    </ScrollView>

</android.support.constraint.ConstraintLayout>